import { ImageSrcPipePipe } from './image-src-pipe.pipe';

describe('ImageSrcPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageSrcPipePipe();
    expect(pipe).toBeTruthy();
  });
});
