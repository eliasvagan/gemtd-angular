import { PercentStringPipePipe } from './percent-string-pipe.pipe';

describe('PercentStringPipePipe', () => {
	it('create an instance', () => {
		const pipe = new PercentStringPipePipe();
		expect(pipe).toBeTruthy();
	});
});
