import { RarityColorPipe } from './rarity-color.pipe';

describe('RarityColorPipe', () => {
  it('create an instance', () => {
    const pipe = new RarityColorPipe();
    expect(pipe).toBeTruthy();
  });
});
